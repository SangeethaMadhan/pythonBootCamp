# program for linear search-from last element to first element in the array
# to check the value present in array
# check if value matches with array element
# if matches - value found. Return True
# if not - go to next element-from last to first
# loop completed - element not found
# return false

def search(arr, value):
    i=len(arr)-1
    while i<=len(arr):
        print(i,arr[i])
        if arr[i]==value:
            return True
        else:
            i=i-1
    return False

arr = [1,3,5,8,9,11,13,15,16,18,19,22,25,26]
value = 13
print(search(arr, value)) # function call